package #(serviceImplPackageName);

import io.jboot.aop.annotation.Bean;
import #(basePackage).#(tableMeta.modelName)Service;
import #(modelPackage).#(tableMeta.modelName);
import okkpp.service.TeddyServiceBase;

import javax.inject.Singleton;

import org.springframework.stereotype.Service;

@Service
@Bean
@Singleton
public class #(tableMeta.modelName)ServiceImpl extends TeddyServiceBase<#(tableMeta.modelName)> implements #(tableMeta.modelName)Service {

}
